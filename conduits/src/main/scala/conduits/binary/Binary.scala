package conduits
package binary

import resourcet.MonadResource
import Source._
import SourceUtil._
import scalaz.effect.IO
import java.io.{FileInputStream, File}

/**
 * User: arjan
 */

object Binary {
//
//  def sourceFile[F[_]](f: File)(implicit MR: MonadResource[F]): Source[F, java.io.InputStream] = {
//    val str = new FileInputStream(f)
//    sourceIO(IO(str), _ => IO(str.close()), )
//
//  }
}
